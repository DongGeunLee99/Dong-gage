# TODO — 나중에 할 일

> 완료 항목은 [log.md](log.md) 참고.

---

## 미완료

- [ ] **카테고리 아이콘 22개 SVG 교체 — 실기기 확인 필요** — 2026-09-09: 손코딩 아이콘(`icons.tsx`의 Path/Circle/Line) 9개를 사용자가 Figma에서 그린 `.svg` 파일 22개(기본 10개 + 커스텀용 12개)로 교체 완료. `react-native-svg-transformer` + `.svgrrc`(black→currentColor 치환)로 정적 import, 기존 `color` prop 방식 그대로 유지해서 화면 코드는 무수정. `tsc`/`expo export`/`expo-doctor` 통과, SVGR 변환 결과(`fill="currentColor"`)도 직접 확인했지만, 실기기/시뮬레이터에서 ① 카테고리 원 배경 안 아이콘이 실제로 잘 보이는지(색상·크기) ② `categoryEdit.tsx` 아이콘 픽커에 22개가 다 나오고 선택 시 정상 반영되는지 ③ 기존 화면(캘린더/리스트/대시보드/관리/예산/고정지출 등)에서 카테고리 아이콘이 깨지지 않는지 확인 필요. 자세한 경위·기술적 함정은 `log.md` 2026-09-09 (1) 참고

- [ ] **"미지정 내역 정리" 화면 — 실기기 확인 필요** — 2026-09-07: 관리 탭에 신규 화면(`src/app/uncategorizedCleanup.tsx`). "카테고리 삭제됨"/"기타로 방치됨" 두 섹션으로 상호명별 그룹핑. `tsc`/`expo-doctor`/`expo export` 통과했지만, 실제로 카테고리를 하나 삭제하거나 기타(소분류 없음) 거래를 만든 뒤 ① 두 섹션이 맞게 나뉘는지 ② 그룹 펼쳤을 때 체크박스 동작 ③ 카테고리 지정 시 체크된 것만 일괄 반영되는지 실기기에서 확인 필요
- [ ] **예산 제외 기능 전체 — 실기기 확인 필요** — 2026-09-07 하루 동안 누적된 변경(로그 (1)(5)(6)(7)(8)(9)(10) 참고): 집계 함수 전면 적용(`monthSummary`/`categoryBreakdown`), 검토 대기함 토글+발신처별(카카오뱅크/네이버페이/기타) 그룹핑(브랜드 색/아이콘 포함)+테스트 항목 버튼, 수입 거래에도 토글 노출, "가리기" 토글을 관리 탭 전역 설정 하나로 통합, 리스트 탭 "제외됨" 세그먼트 추가, 관리 탭 "내 이름(이체 매칭용)" 입력 + 검토함 자동토글, 캘린더/리스트 "제외됨" 태그 표시. `tsc`/`expo-doctor`/`expo export`는 전부 통과했으나(정적 검증 한계가 있었던 전례 — react-native-tab-view 건 — 있음), 실기기에서 ① 테스트 버튼으로 검토함에 항목 생성 ② 카카오/네이버 그룹 라벨·색·아이콘 확인 ③ 예산 제외 토글(수입 포함) 켠 거래가 대시보드/예산/캘린더·리스트 총액에서 실제로 빠지는지 ④ 관리 탭 전역 토글이 캘린더·리스트 양쪽에 동시 반영되는지 ⑤ 리스트 "제외됨" 세그먼트가 가리기 설정과 무관하게 항상 보이는지 ⑥ "내 이름" 입력 후 이체 문자가 검토함에서 토글 미리 켜진 채로 뜨는지 ⑦ "가리기"를 끈 상태에서 캘린더/리스트에 예산제외 거래 옆 "제외됨" 태그가 잘 보이고 다른 텍스트와 안 겹치는지 확인 필요
- [ ] **고정지출 재설계(Phase 2) — SQL 실행 + Edge Function 재배포 + 실기기 확인 필요** — 2026-09-08: 고정지출에 카테고리/상호명 연결, SMS 자동매칭 엔진 추가 완료(로그 (12) 참고). 남은 것: ① `schema_v6_fixed_expense_matching.sql` 실행 ② `supabase functions deploy sms-ingest --no-verify-jwt` 재배포 ③ 넷플릭스/헬스장 실제 상호명 확인 후 앱(고정지출 수정)에서 채워넣기 ④ 실기기에서 카테고리 선택 화면 정상 동작, 통신비(LGUPLUS) SMS 왔을 때 자동매칭되는지 확인
- [ ] **카테고리 재정리 SQL — 실행 후 앱에서 확인 필요** — 2026-09-08: `schema_v5_category_cleanup.sql` 실행 완료(사용자 확인). 기존 테이블 UPDATE/INSERT라 RLS 때문에 API로 재확인 불가 — 관리 탭에서 ① 기타 소분류가 비었는지 ② 주거·통신에 구독서비스가 추가됐는지 ③ 카페 카테고리(소분류 3개, 임시 아이콘)가 생겼는지 확인 필요
- [ ] **Expo SDK 57 업그레이드 — 실기기 재확인 필요** — 2026-09-06: `react-native-tab-view` 제거가 실수였음이 실기기 실행에서 드러나 복구함(`expo-router/js-top-tabs`의 숨은 런타임 의존성). `tsc`/`expo-doctor`/`expo export`는 전부 통과했지만 이 셋으로는 못 잡는 종류의 에러였으므로, 4탭(캘린더/리스트/대시보드/관리)과 상단 탭바가 실기기에서 정상 동작하는지 다시 확인 필요

- [ ] **AI 어드바이저 — 속도 개선 재확인** — 2026-09-03: 실사용 테스트에서 "답변 품질은 나쁘지 않은데 15초 걸린다"는 피드백. 원인은 `gemini-3.7-flash`의 기본 thinking 수준(medium)으로 추정, `generationConfig.thinking_level: 'minimal'`로 낮추고 도구 병렬 실행(`Promise.all`)까지 적용해 재배포했으나, 실제 체감 속도가 개선됐는지는 아직 재확인 못 함
- [ ] **AI 정산 — 거래 검색 범위 확장** — 지금은 "오늘" 거래만 후보로 보여줌(`TODAY` 상수 기준). 어제 야근 회식처럼 날짜가 걸치는 경우나 여러 날에 걸친 정산은 아직 처리 못 함
- [ ] **iOS Icon Composer(`assets/expo.icon`) 재도입 검토** — SDK 54의 새 iOS 아이콘 포맷(레이어/그라데이션/틴트 지원)인데, `icon.json` 스키마가 공식 문서에 상세히 설명되어 있지 않고 보통 Xcode의 Icon Composer 앱으로 만든다. 지금은 리스크를 피하려고 `ios.icon`을 평범한 PNG(`icon.png`)로 되돌려둠. 나중에 iOS 전용 다이나믹 아이콘이 필요해지면 Icon Composer 앱으로 직접 만들어서 교체
- [ ] **네이버페이 문자 파서 — 실기기 검증 필요** — 2026-09-06: `parseNaverPaySms()` 추가(`supabase/functions/sms-ingest/parse.ts`) + 재배포 완료, 로컬 정규식 테스트(스크래치패드, 8케이스)만 통과한 상태. 아직 안 한 것: ① 아이폰 단축어에 네이버 알림 발신번호용 자동화 신규 추가 ② 실제 수신 문자로 `saved: true` + 카테고리 매칭 확인 ③ "결제완료안내" 외 다른 네이버페이 문자 형식(결제취소안내 등)이 실제로 어떻게 오는지 확인 후 필요시 처리 범위 확장
- [ ] **문자 파서 — 그 외 은행/카드사 대응** — 카카오뱅크·네이버페이 다음으로 추가할 발신처가 생기면 형식 확인 후 `parse.ts`에 파서 추가 + `SMS_PARSERS` 배열에 등록
- [ ] **예산/고정지출 Supabase 이전 — SQL 마이그레이션 실행 + 실기기 확인 필요** — 2026-09-07: `budgetsContext.tsx`/`fixedExpensesContext.tsx`를 categories와 같은 패턴으로 재작성 완료(`tsc`/`expo-doctor`/`expo export` 통과). 아직 안 한 것: ① `supabase/schema_v4_budgets_fixed_expenses.sql`을 Supabase SQL Editor에서 실행(안 하면 예산/고정지출 화면이 빈 상태로 뜸) ② 실기기에서 예산/고정지출 수정 후 앱 재시작해도 유지되는지 확인 ③ 관리 탭에 새로 추가된 "내 이름(이체 매칭용)" 입력 후, 그 이름이 들어간 이체 문자가 검토함에서 예산제외 토글이 미리 켜진 채로 뜨는지 + 승인 후 실제로 예산 집계에서 빠지는지 확인
- [ ] Expo Go 개발 중 카카오 로그인 테스트하려면 매번 `npx expo start --tunnel`로 켜야 함 (일반 모드는 `exp://<로컬IP>` 리다이렉트가 Supabase 허용 목록과 매칭 안 되는 알려진 버그)

---

## 완료

<!-- 완료 시 여기로 이동, 날짜와 함께 -->

- [x] **AI 어드바이저 — 실제 대화 테스트 + 속도 개선** — 2026-09-03: 사용자가 실기기에서 질문해보고 "나쁘진 않은데 느리다(15초)"고 확인 — 답변 품질 자체는 문제없음. 원인 조사 후 `thinking_level: 'minimal'`(불필요한 추론 낮춤) + 도구 호출 병렬화로 재배포. 실제 체감 개선 여부 재확인은 todo에 남김
- [x] **당겨서 새로고침 + 완료 피드백 UX 다듬기** — 2026-09-03: 캘린더/리스트/대시보드/관리 4탭에 pull-to-refresh 추가(`transactionsContext`/`categoriesContext`의 새 `refresh()` 사용). 사용자 실기기 테스트로 여러 번 반복 조정 — ① "완료 신호가 필요하다"는 요청으로 토스트 추가 → ② "화면 하단 중앙에 페이드로" 스펙 구체화 → ③ "손을 뗄 때 시작돼야 자연스럽다"는 지적으로 원인 규명(네이티브 RefreshControl은 당김 임계값 통과 즉시 실행되는 게 정상 동작 — 대신 우리가 붙이는 완료 신호만 release 시점까지 게이팅하는 `useRefreshFeedback` 훅으로 해결) → ④ 콘텐츠 짧을 때 당길 여백이 없는 문제를 `flexGrow:1`로 수정 → ⑤ 페이드에 300ms 딜레이 추가. 최종 사용자 확인("아주 좋아") 후 커밋
- [x] **탭바 5개→4개 병합 + 헤더 버튼 재배치/확대** — 2026-09-03: 관리+설정 탭 병합(`managementStyles.ts` 하나로 통합), 헤더 아이콘 버튼을 탭마다 1개씩(캘린더=검토대기/리스트=정산하기/대시보드=AI상담)으로 정리하고 원형→가로로 넓은 사각형(112x32)에 텍스트 라벨 추가, 탭바 텍스트 라벨도 제거해 화면을 넓게 씀. 사용자 실기기 피드백으로 버튼 크기/텍스트 반복 조정 후 확인("좋네")
- [x] **AI 활용처 재검토 → "AI 가계부 도우미"(자연어 질의) 설계·구현** — 2026-09-03: "AI 좀 별로네" 이후 후보(SMS 카테고리 추론/가맹점명 정리/월간 리뷰/예산 예측/카드 추천/영수증 OCR/자연어 질의) 논의 끝에, "클로드 코드처럼" 스스로 여러 번 조회하며 답하는 자연어 질의를 1순위로 결정. LLM은 Gemini 무료 티어 유지(필요해지면 Claude API 전환은 별도 결정 — 구독과 API는 별개 상품/과금이라는 점 확인함). 상세는 log.md 참고
- [x] **SMS 자동입력 파이프라인 — 배포 및 단축어 연결** — 2026-09-02: 스키마 적용(SQL Editor) → `sms-ingest` 함수 배포(`--no-verify-jwt`) → 앱에서 토큰 발급 → iOS 단축어("메시지를 받을 때" → 웹 요청 POST) 작성까지 완료. 단축어 ▶ 수동 실행으로 샘플 문자를 넣어 `saved: true` + 카테고리 자동 매칭(`exact`)까지 확인. 실제 수신 문자로 도는 것까지는 사용자가 QC 진행 중
- [x] **AI 정산 — LLM 연동(Gemini) 및 배포** — 2026-09-02: Edge Function `ai-settlement` 배포 + 앱 "AI로 채우기" UI. 최신 flash 모델이 무료 티어 503을 자주 뱉어서 모델 폴백 체인 + 모델당 25s 타임아웃 도입. 실기기에서 사용해봤으나 결과가 기대에 못 미친다는 평가 → 후속은 "AI 활용처 재검토" 항목으로 이어짐
- [x] **오늘 날짜 하드코딩 제거** — 2026-09-02: `transactionsContext`의 `TODAY`가 `2026-08-30` 고정값이라 캘린더가 8월 30일로 열리던 문제. 앱 시작 시 기기 날짜를 계산하도록 변경(캘린더 포커스, 새 거래 기본 날짜, AI 정산의 "오늘 거래" 후보가 모두 이 값을 봄)
- [x] **"이체(계좌간 전환)" 거래 처리 방식 결정** — 2026-09-02: 문자 파이프라인 설계하면서 정리됨. 이체·충전 문자(카카오페이/저금통 등)도 걸러내지 않고 전부 `pending_review`로 저장한 뒤, 앱의 검토 화면에서 사람이 추가/삭제를 판단하는 방식으로 결정. 가맹점명 기반 자동 제외 목록은 두지 않음(오탐 위험 + 검토 단계가 이미 있음)

- [x] **앱 아이콘(홈 화면/스플래시) 제작** — 2026-08-31: 사용자가 새싹(sprout) 모양 아이콘(`assets/images/icon-source.svg`)을 선택, sharp로 iOS/Android/web/스플래시용 PNG 일괄 생성. iOS는 신형 Icon Composer 번들(`assets/expo.icon`) 대신 평범한 PNG로 단순화(스키마 문서 미비로 리스크 회피, todo에 재검토 항목 남김). 스플래시/Android 배경색을 기존 파란색(`#208AEF`/`#E6F4FE`)에서 흰색으로 통일
- [x] **새 카테고리 아이콘 손그림 재작업** — 2026-08-31: 주거·통신/건강/여가·문화/경조사·비정기/금융 5개를 기존 식비/교통/쇼핑/기타와 같은 손그림 SVG 스타일(20x20 viewBox, `stroke` 기반, `strokeWidth 1.7`)로 재작업. `@expo/vector-icons`(Ionicons) 의존 제거 — iOS 번들 크기 약 380KB 감소. 웹 프리뷰가 SSR 버그로 막혀 있어 기기(Expo Go)에서 육안 확인 필요
- [x] **Git 저장소 초기화** — 2026-08-31: 이후 작업분(카테고리/거래 Supabase 이전, tabs 네비게이션, i18n 등) 전부 커밋 완료. `8월 테스트.xlsx`(개인 실 데이터)는 gitignore 처리
