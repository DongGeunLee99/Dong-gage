@AGENTS.md

## 작업 운영 파일

`.claude/` 폴더에 진행 상황 기록:
- `log.md` — 작업 내역 (무엇을 했는지, 왜 했는지, 원인/해결책까지 서술. 최신순 위)
- `todo.md` — 나중에 진행할 일 체크리스트 (완료 시 날짜와 함께 완료 섹션으로 이동, 삭제하지 않음)

## 코드 수정 전 승인 규칙

- **명시적인 진행 승인 없이는 파일을 수정하지 않는다.** "이렇게 하면 어떨까?", "~하고 싶어", "~하면 좋겠어" 같은 의견 제시·의도 표현은 승인이 아니다. 실행을 요청하는 동사("수정해줘", "바꿔줘", "진행해줘" 등)가 있어도 **같은 메시지 안에 질문("어떻게 생각해?" 등)이 하나라도 섞여 있으면 메시지 전체를 논의로 취급**하고, 질문에 답만 한 뒤 별도의 명확한 승인 메시지를 기다린다. 한 메시지 안에서 일부 문장만 골라 실행하지 않는다.
- **큰 작업은 진행 전 계획표를 먼저 제시하고 승인받는다.** 다음 중 하나라도 해당하면 "큰 작업"으로 본다: 신규 파일 3개 이상 또는 수정 파일 8개 이상, 라이브러리 추가/교체, 데이터 구조·상태관리·폴더 구조 변경, 되돌리기 어려운 작업(대량 삭제, 마이그레이션). 기능 자체는 이미 승인받았더라도 이 기준에 걸리면 실제 작업 착수 전에 계획표(대안 2개 이상+추천, 생성/수정 파일 목록, 검증 방법)를 먼저 보여주고 승인받는다.
- 세션이 "Auto Mode" 등 확인 절차를 최소화하는 모드로 동작 중이더라도 위 두 규칙은 예외 없이 적용된다.

